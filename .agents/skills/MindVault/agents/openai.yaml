interface:
  display_name: "MindVault"
  short_description: "Repo-specific patterns and workflows for MindVault"
  default_prompt: "Use the MindVault repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true