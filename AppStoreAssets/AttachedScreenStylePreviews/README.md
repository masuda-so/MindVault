# Attached Screen Style App Store Previews

Previews modeled after the user-provided Mynaportal real app screen images: pale blue background, centered iPhone frame, and the actual application screen as the primary content.

- MindVault: `MindVault/iPhone65/`
- 小さなありがとう日記: `SmallThanksDiary/iPhone65/`
- Each app has 10 iPhone screenshots at 1284 x 2778 px, exported as PNG and JPEG, plus a contact sheet.
- All framed app screens in this batch come from actual Simulator captures saved under `../ActualScreenSources/`.
- 今日のひとコマ: excluded; user indicated it is superseded by 小さなありがとう日記. Deletion status is tracked in `../AppStoreConnectDeletionLog.md`.

The generator only frames captured app screens. It does not draw replacement app UI for missing screens.
